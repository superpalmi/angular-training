"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
require("reflect-metadata");
const shared_1 = require("./shared");
function isObject(target) {
    if (target === undefined || target === null) {
        return false;
    }
    return typeof target === 'object';
}
exports.isObject = isObject;
function isArray(target) {
    if (target === undefined || target === null) {
        return false;
    }
    if (target === Array) {
        return true;
    }
    return Object.prototype.toString.call(target) === '[object Array]';
}
exports.isArray = isArray;
function isPrimitiveType(target) {
    if (target === undefined || target === null) {
        return false;
    }
    return !!(['string', 'boolean', 'number']
        .indexOf((typeof target)) > -1 || (target instanceof String || target === String ||
        target instanceof Number || target === Number ||
        target instanceof Boolean || target === Boolean));
}
exports.isPrimitiveType = isPrimitiveType;
function getPropertyMetadata(target, propertyKey) {
    return Reflect.getMetadata(shared_1.JSON_PROPERTY_KEY, target, propertyKey);
}
exports.getPropertyMetadata = getPropertyMetadata;
