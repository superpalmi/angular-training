import { TypeselectorPipe } from './typeselector.pipe';

describe('TypeselectorPipe', () => {
  it('create an instance', () => {
    const pipe = new TypeselectorPipe();
    expect(pipe).toBeTruthy();
  });
});
